<!DOCTYPE html>
<html lang="es">
<head>
    <%- include('./partials/head.ejs') %> 
</head>
<body>
    <div class="container-fluid px-0 body-login">
        <div class="container-logo-inputs col-6 d-flex flex-column justify-content-center py-auto">
            <div class="d-flex flex-row justify-content-between align-items-start col-12 mx-auto">
                
                <div class="container-login-signup mx-auto d-flex flex-row justify-content-start mb-5 col-5">
                    <h2 class="text-bold text-white">Crear cuenta</h2>
                </div>
                
                
                <div class="container-login-inputs d-flex flex-column justify-content-start align-items-end col-6">
                    <form action="/signup" method="POST">
                        <!-- email -->
                        <div class="d-flex flex-column justify-content-start align-items-start col-12">
                            <span class="input-span">Correo electrónico</span>
                            <input type="email" name="email" placeholder="Correo electrónico" class="col-12 mx-auto mt-1 mb-2" value="<%= typeof old != 'undefined' ? old.email : '' %>">
                        </div>
                        <% if(typeof errors != 'undefined' && errors.email){%>
                            <span class="text-danger" > <%=errors.email.msg %></span>
                        <%}%>
                        <!-- Nombre y apellido -->
                        <div class="d-flex flex-row justify-content-between align-items-center col-12">
                            <!-- Nombre -->
                            <div class="d-flex flex-column justify-content-start align-items-start col-6">
                                <span class="input-span">Nombre</span>
                                <input type="text" name="name" placeholder="Nombre" class="col-10 mt-1 mb-2 ml-0">
                                <% if(typeof errors != 'undefined' && errors.name){%>
                                    <span class="text-danger" > <%=errors.name.msg %></span>
                                <%}%>
                            </div>
                            <!-- Apellido -->
                            <div class="d-flex flex-column justify-content-start align-items-start col-6">
                                <span class="input-span">Apellido</span>
                                <input type="text" name="lastName" placeholder="Apellido" class="col-12 mt-1 mb-2 ml-0">
                                <% if(typeof errors != 'undefined' && errors.lastName){%>
                                    <span class="text-danger" > <%=errors.lastName.msg %></span>
                                <%}%>
                            </div>
                        </div>
                        <!-- Password -->
                        <div class="d-flex flex-column justify-content-start align-items-start col-12">
                            <span class="input-span">Contraseña</span>
                            <input type="password" name="password" placeholder="Contraseña" class="col-12 mx-auto mt-1 my-2">
                            <% if(typeof errors != 'undefined' && errors.password){%>
                                <span class="text-danger" > <%=errors.password.msg %></span>
                            <%}%>
                        </div>
                        <!-- Confirm password -->
                        <div class="d-flex flex-column justify-content-start align-items-start col-12">
                            <span class="input-span">Confirmar contraseña</span>
                            <input type="password" name="confirmPassword" placeholder="Contraseña" class="col-12 mx-auto mt-1 my-2">
                            <% if(typeof errors != 'undefined' && errors.confirmPassword){%>
                                <span class="text-danger" > <%=errors.confirmPassword.msg %></span>
                            <%}%>
                        </div>
                        <div class="col-12 d-flex flex-row justify-content-around align-items-end">
                            <a href="/" class="login-signup-back col-6">Volver</a>
                            <!-- Submit registration -->
                            <button type="submit" class="mt-3 mb-0 btn btn-outline-light col-6">Crear cuenta</button>
                        </div>
                    </form>
                    
                </div>
            </div>
            
        </div>
    </div>
    <!-- Bootstrap js -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/js/bootstrap.bundle.min.js" integrity="sha384-ygbV9kiqUc6oa4msXn9868pTtWMgiQaeYH7/t7LECLbyPA2x65Kgf80OJFdroafW" crossorigin="anonymous"></script>
</body>
</html>
